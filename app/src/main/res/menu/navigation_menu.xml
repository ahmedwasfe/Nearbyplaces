<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/nav_market"
        android:title="Market"
        android:icon="@drawable/ic_market"
        android:enabled="true"
        app:showAsAction="ifRoom"/>

    <item android:id="@+id/nav_school"
        android:title="School"
        android:icon="@drawable/ic_school"
        android:enabled="true"
        app:showAsAction="ifRoom"/>

    <item android:id="@+id/nav_restaurant"
        android:title="Restaurant"
        android:icon="@drawable/ic_restaurant"
        android:enabled="true"
        app:showAsAction="ifRoom"/>

    <item android:id="@+id/nav_hospital"
        android:title="Hospital"
        android:icon="@drawable/ic_hospital"
        android:enabled="true"
        app:showAsAction="ifRoom"/>
</menu>